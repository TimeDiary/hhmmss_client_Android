<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="kr.co.hhmmss.hhmmss">

    <!--README-->
    <!--Line:13 android:allowBackup="true"-->
    <!--Line:14 android:fullBackupContent="@xml/backup_descriptor"-->
    <!--Line:20 tools:ignore="GoogleAppIndexingWarning"-->
    <!--Manifest 권고로 인한 속성 추가, 어떤 역할인지는 모르므로 주의-->

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".auth.BaseActivity" />
        <activity android:name=".auth.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>